Title: <%= @job.title %>
<br>

Description: <%= @job.description %>
<br>

Level of Interest: <%= @job.level_of_interest %>
<br>

City: <%= @job.city %>
<br>

<p><%= link_to "Edit", edit_job_path(@job) %></p>
<p><%= link_to "Delete", job_path(@job), method: :delete,
       data: {confirm: "Do you really wanna delete  the job?"}%></p>

Company: <%= link_to @job.company.name, company_path(@job.company) %>

<%= render partial: 'comment', collection: @job.comments.reverse %>
<%= render partial: 'comments/form' %>
